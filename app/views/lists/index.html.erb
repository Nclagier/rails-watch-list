<h1> Summary of lists <%= link_to "Create a List", new_list_path(@list), class: "btn btn-warning" %></h1>

<ul>
<% @lists.each do |list| %>
<li><%=link_to list.name, list_path(list)%></li>
<% end %>
</ul>
